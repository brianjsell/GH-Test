#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../curl-trace-parser/bin/curl-trace-parser" "$@"
  ret=$?
else 
  node  "$basedir/../curl-trace-parser/bin/curl-trace-parser" "$@"
  ret=$?
fi
exit $ret
